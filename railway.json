{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/prepare-railway.js && npx prisma generate --schema=server/src/prisma/schema.prisma && npx prisma db push --schema=server/src/prisma/schema.prisma --accept-data-loss && npm run build:server && npm run build:client"
  },
  "deploy": {
    "startCommand": "node server/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
